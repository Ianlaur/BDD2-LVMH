# Core data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# Machine learning & clustering
scikit-learn>=1.3.0

# Keyphrase extraction (deterministic NLP)
yake>=0.4.8
rake-nltk>=1.0.6
nltk>=3.8.0

# Embeddings (multilingual, non-generative)
sentence-transformers>=2.2.0
transformers>=4.30.0
torch>=2.0.0

# TensorFlow for multi-task supervised learning
tensorflow>=2.15.0
tensorflow-metal>=1.1.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# Dimensionality reduction & visualization
umap-learn>=0.5.0
plotly>=5.15.0

# YAML parsing for playbooks
pyyaml>=6.0

# API server for remote access
fastapi>=0.104.0
uvicorn[standard]>=0.24.0